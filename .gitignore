# --- PromptWars: keep secrets and build output out of git ---
node_modules/
.env
.env.local
*.local
dist/
**/dist/
*.log
.DS_Store
.pgdata/
# --- end snippet ---

# --- Local-only PromptWars playbook (keep private; not committed) ---
CLAUDE.md
SCORING.md
# --- end playbook ---

# Note: README.md and .env.example ARE committed — the README is part of how the
# submission is judged, and .env.example documents required vars for reviewers.

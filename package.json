{
  "name": "anchor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Anchor — a GenAI mental-wellness companion that reads between the lines of a student's journal to surface hidden stress triggers, match an evidence-based coping exercise, and keep a crisis safety net.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace web",
    "start": "npm start --workspace server",
    "test": "npm test --workspace server && npm test --workspace web",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "eslint": "^9.39.4",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.7.0",
    "prettier": "^3.8.5"
  }
}
